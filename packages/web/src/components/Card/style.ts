import styled from 'styled-components'

export const CardContainer = styled.div`
  width: 100%;
  max-width: 550px;
  max-height: 80%;
  background: var(--bg-primary);
  box-shadow: 0px 10px 40px rgba(0, 0, 0, 0.2), 0px 10px 15px rgba(0, 0, 0, 0.2);
  border-radius: 10px;
`
