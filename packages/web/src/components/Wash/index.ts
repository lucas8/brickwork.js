import styled from 'styled-components'

const Wash = styled.div`
  z-index: 1;
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(var(--bg-primary-rgb), 0.2);
`

export default Wash
